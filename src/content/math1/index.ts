import type { ConceptData } from "@/types/concept";
import { trigFunctions } from "./trig-functions";
import { expLog } from "./exp-log";
import { sequences } from "./sequences";

export const math1Concepts: ConceptData[] = [
  trigFunctions,
  expLog,
  sequences,
];
